<resources>

    <!-- Base application theme. -->
    <style name="MyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryDark">@color/main_color_2</item>
        <item name="colorAccent">@color/main_color</item>
    </style>

    <style name="CustomButton">
        <item name="android:backgroundTint">@color/main_color_3</item>
        <item name="android:colorButtonNormal">@color/main_color_3</item>
    </style>

    <style name="CustomButton2">
        <item name="android:backgroundTint">@color/main_color</item>
        <item name="android:colorButtonNormal">@color/main_color</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
